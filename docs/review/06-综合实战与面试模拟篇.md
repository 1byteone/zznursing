# 智颐养老系统 - 面试项目实战练习题（六）

> 本练习题集聚焦综合实战与面试模拟，每道题包含完整代码参考、单元测试、详细解析和面试思维链。

---

## 六、综合实战与面试模拟篇（5题）

### 第26题：全局异常处理设计

**题目描述**：
请设计并实现项目的全局异常处理机制，包括：自定义业务异常、全局异常处理器、统一响应格式。

**核心考察点**：
- Spring全局异常处理
- 异常分类设计
- 统一响应格式

**异常处理架构**：

```
┌─────────────────────────────────────────────────────────────┐
│                        异常来源                              │
├─────────────────────────────────────────────────────────────┤
│ BaseException    MethodArgumentNotValid    DuplicateKey/SQL │
│ (业务异常)       (参数校验异常)            (数据库异常)      │
│                             NPE/IO/网络                     │
│                             (系统异常)                       │
└──────────┬────────┬────────┬────────┬────────┬──────────────┘
           │        │        │        │        │
           v        v        v        v        v
┌─────────────────────────────────────────────────────────────┐
│                GlobalExceptionHandler                       │
│               @RestControllerAdvice                        │
├─────────────────────────────────────────────────────────────┤
│ @ExceptionHandler(BaseException)        → user msg          │
│ @ExceptionHandler(Validation)           → param err         │
│ @ExceptionHandler(DuplicateKey)         → field msg         │
│ @ExceptionHandler(Exception)            → general msg       │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       v
┌─────────────────────────────────────────────────────────────┐
│                    统一响应格式                              │
├──────────────────────────────┬──────────────────────────────┤
│ 后台: AjaxResult             │ 小程序: R<T>                 │
│ {code, msg, data}            │ {code, msg, data}            │
└──────────────────────────────┴──────────────────────────────┘
```

**核心代码实现**：

<details>
<summary><b>BaseException.java - 自定义业务异常</b></summary>

```java
// BaseException.java - 自定义业务异常
public class BaseException extends RuntimeException {
    private static final long serialVersionUID = 1L;

    /** 错误码 */
    private Integer code;

    /** 错误消息 */
    private String message;

    /** 错误详情 */
    private String detail;

    public BaseException(String message) {
        super(message);
        this.code = 500;
        this.message = message;
    }

    public BaseException(Integer code, String message) {
        super(message);
        this.code = code;
        this.message = message;
    }

    public BaseException(String message, Throwable cause) {
        super(message, cause);
        this.code = 500;
        this.message = message;
    }

    // getter/setter...
}
```

</details>

<details>
<summary><b>GlobalExceptionHandler.java - 全局异常处理器</b></summary>

```java
// GlobalExceptionHandler.java - 全局异常处理器
@RestControllerAdvice
@Slf4j
public class GlobalExceptionHandler {

    /**
     * 处理业务异常
     */
    @ExceptionHandler(BaseException.class)
    public AjaxResult handleBaseException(BaseException e) {
        log.error("业务异常: code={}, message={}", e.getCode(), e.getMessage());
        return AjaxResult.error(e.getMessage());
    }

    /**
     * 处理参数校验异常
     */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public AjaxResult handleValidationException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
                .map(FieldError::getDefaultMessage)
                .collect(Collectors.joining(", "));
        log.warn("参数校验失败: {}", message);
        return AjaxResult.error("参数校验失败: " + message);
    }

    /**
     * 处理HTTP消息不可读异常（如JSON格式错误）
     */
    @ExceptionHandler(HttpMessageNotReadableException.class)
    public AjaxResult handleHttpMessageNotReadable(HttpMessageNotReadableException e) {
        log.warn("请求体格式错误: {}", e.getMessage());
        return AjaxResult.error("请求参数格式错误，请检查JSON格式");
    }

    /**
     * 处理请求方法不支持异常
     */
    @ExceptionHandler(HttpRequestMethodNotSupportedException.class)
    public AjaxResult handleMethodNotSupported(HttpRequestMethodNotSupportedException e) {
        log.warn("不支持的请求方法: {}", e.getMethod());
        return AjaxResult.error("不支持的请求方法: " + e.getMethod());
    }

    /**
     * 处理数据库唯一约束冲突
     */
    @ExceptionHandler(DuplicateKeyException.class)
    public AjaxResult handleDuplicateKey(DuplicateKeyException e) {
        log.error("数据重复: {}", e.getMessage());
        // 提取重复字段信息
        String message = extractDuplicateField(e.getMessage());
        return AjaxResult.error(message);
    }

    /**
     * 处理空指针异常
     */
    @ExceptionHandler(NullPointerException.class)
    public AjaxResult handleNullPointer(NullPointerException e) {
        log.error("空指针异常", e);
        return AjaxResult.error("系统内部错误，请联系管理员");
    }

    /**
     * 处理所有未捕获异常
     */
    @ExceptionHandler(Exception.class)
    public AjaxResult handleException(Exception e) {
        log.error("系统异常", e);
        return AjaxResult.error("系统繁忙，请稍后重试");
    }

    /**
     * 提取数据库重复字段信息
     */
    private String extractDuplicateField(String errorMessage) {
        if (errorMessage == null) {
            return "数据已存在，请勿重复操作";
        }
        if (errorMessage.contains("id_card")) {
            return "该身份证号已存在";
        }
        if (errorMessage.contains("phone")) {
            return "该手机号已存在";
        }
        if (errorMessage.contains("device_name")) {
            return "该设备名称已存在";
        }
        return "数据已存在，请勿重复操作";
    }
}
```

</details>

<details>
<summary><b>AjaxResult.java - 统一响应格式</b></summary>

```java
// AjaxResult.java - 统一响应格式
public class AjaxResult extends HashMap<String, Object> {
    private static final long serialVersionUID = 1L;

    /** 状态码 */
    public static final String CODE_TAG = "code";

    /** 返回内容 */
    public static final String MSG_TAG = "msg";

    /** 数据对象 */
    public static final String DATA_TAG = "data";

    public AjaxResult() {
    }

    public AjaxResult(int code, String msg) {
        super.put(CODE_TAG, code);
        super.put(MSG_TAG, msg);
    }

    public AjaxResult(int code, String msg, Object data) {
        super.put(CODE_TAG, code);
        super.put(MSG_TAG, msg);
        if (data != null) {
            super.put(DATA_TAG, data);
        }
    }

    /**
     * 返回成功消息
     */
    public static AjaxResult success() {
        return AjaxResult.success("操作成功");
    }

    public static AjaxResult success(String msg) {
        return new AjaxResult(HttpStatus.SUCCESS, msg);
    }

    public static AjaxResult success(Object data) {
        return new AjaxResult(HttpStatus.SUCCESS, "操作成功", data);
    }

    /**
     * 返回错误消息
     */
    public static AjaxResult error() {
        return AjaxResult.error("操作失败");
    }

    public static AjaxResult error(String msg) {
        return new AjaxResult(HttpStatus.ERROR, msg);
    }

    public static AjaxResult error(int code, String msg) {
        return new AjaxResult(code, msg);
    }

    /**
     * 链式添加数据
     */
    @Override
    public AjaxResult put(String key, Object value) {
        super.put(key, value);
        return this;
    }
}

// R.java - 小程序端统一响应（简化版）
@Data
public class R<T> {
    private int code;
    private String msg;
    private T data;

    public static <T> R<T> ok() {
        return ok(null);
    }

    public static <T> R<T> ok(T data) {
        R<T> r = new R<>();
        r.setCode(200);
        r.setMsg("操作成功");
        r.setData(data);
        return r;
    }

    public static <T> R<T> fail(String msg) {
        R<T> r = new R<>();
        r.setCode(500);
        r.setMsg(msg);
        return r;
    }
}
```

</details>

**单元测试**：

<details>
<summary><b>GlobalExceptionTest.java - 单元测试</b></summary>

```java
@SpringBootTest
@AutoConfigureMockMvc
public class GlobalExceptionTest {

    @Autowired
    private MockMvc mockMvc;

    /**
     * 测试业务异常处理
     */
    @Test
    public void testBusinessException() throws Exception {
        mockMvc.perform(get("/test/business-exception"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(500))
            .andExpect(jsonPath("$.msg").value("业务异常测试"));
    }

    /**
     * 测试参数校验异常
     */
    @Test
    public void testValidationException() throws Exception {
        String invalidJson = "{\"name\": \"\"}"; // 空名称

        mockMvc.perform(post("/test/validate")
                .contentType(MediaType.APPLICATION_JSON)
                .content(invalidJson))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(500))
            .andExpect(jsonPath("$.msg").value(
                org.hamcrest.Matchers.containsString("参数校验")
            ));
    }

    /**
     * 测试JSON格式错误
     */
    @Test
    public void testMalformedJson() throws Exception {
        mockMvc.perform(post("/test/validate")
                .contentType(MediaType.APPLICATION_JSON)
                .content("not a json"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(500));
    }

    /**
     * 测试数据库唯一约束异常
     */
    @Test
    public void testDuplicateKeyException() throws Exception {
        // 模拟重复插入
        // 实际测试需要数据库支持
    }

    /**
     * 测试自定义异常
     */
    @Test
    public void testCustomException() {
        BaseException ex = new BaseException("自定义错误");
        assertThat(ex.getCode()).isEqualTo(500);
        assertThat(ex.getMessage()).isEqualTo("自定义错误");

        BaseException ex2 = new BaseException(400, "参数错误");
        assertThat(ex2.getCode()).isEqualTo(400);
        assertThat(ex2.getMessage()).isEqualTo("参数错误");
    }
}
```

</details>

**详细解析**：

1. **异常分类设计**：
   - **BaseException**：业务异常，用户可理解
   - **参数校验异常**：前端参数问题
   - **系统异常**：NPE、数据库异常等，不暴露详情

2. **异常处理原则**：
   - 业务异常：返回友好提示
   - 系统异常：记录日志，返回通用提示
   - 不暴露堆栈信息给前端

3. **统一响应格式**：
   - 后台：AjaxResult {code, msg, data}
   - 小程序：R<T> {code, msg, data}
   - 好处：前端统一处理，降低沟通成本

**面试思维链**：

```
面试官问：项目异常处理怎么做的？

思维链：
1. 先说异常分类：业务异常 vs 系统异常
2. 再说全局处理：@RestControllerAdvice
3. 重点说安全：不暴露系统内部信息

回答要点：
"异常处理分三层：
1. 自定义BaseException：业务异常，用户可理解
2. @RestControllerAdvice全局处理器：统一捕获和响应
3. 统一响应格式：AjaxResult {code, msg, data}

关键原则：
- 业务异常返回友好提示
- 系统异常记录日志，返回通用提示
- 绝不暴露堆栈信息给前端
- 数据库唯一约束异常提取具体字段提示
"

追问应对：
Q: 如果异常没有被全局处理器捕获怎么办？
A: 1. Spring Boot有默认异常处理，返回500错误页面
   2. 可通过@ExceptionHandler(Exception.class)兜底
   3. 建议定期排查是否有漏网的异常类型

Q: 异常处理会影响性能吗？
A: 1. 正常流程无额外开销，只在异常时触发
   2. 日志记录用异步方式，不阻塞主流程
   3. 异常是少数情况，性能影响可忽略
```

---

### 第27题：MyBatis-Plus高级用法

**题目描述**：
请展示项目中MyBatis-Plus的高级用法，包括：LambdaQueryWrapper条件构造、分页查询、批量操作、逻辑删除、自动填充。

**核心代码实现**：

<details>
<summary><b>LambdaQueryWrapper条件构造 & 分页查询</b></summary>

```java
// 1. LambdaQueryWrapper条件构造
// 类型安全，避免硬编码字段名
LambdaQueryWrapper<Elder> wrapper = new LambdaQueryWrapper<>();
wrapper.eq(Elder::getStatus, ElderStatus.CHECKED_IN.getCode())  // 入住中
       .like(StringUtils.isNotEmpty(name), Elder::getName, name) // 条件性查询
       .ge(Elder::getCreateTime, startTime)                      // 大于等于
       .le(Elder::getCreateTime, endTime)                        // 小于等于
       .orderByDesc(Elder::getCreateTime);                       // 排序

List<Elder> elders = elderMapper.selectList(wrapper);

// 2. 分页查询
// 配置分页插件
@Configuration
public class MybatisPlusConfig {
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor(DbType.MYSQL));
        return interceptor;
    }
}

// 使用分页
Page<Device> page = new Page<>(pageNum, pageSize);
Page<Device> result = deviceMapper.selectPage(page,
    Wrappers.<Device>lambdaQuery()
        .eq(Device::getLocationType, locationType)
        .orderByDesc(Device::getCreateTime)
);
```

</details>

<details>
<summary><b>批量操作 & 逻辑删除</b></summary>

```java
// 3. 批量操作
// 批量插入
List<DeviceData> dataList = new ArrayList<>();
// ... 构建数据
deviceDataService.saveBatch(dataList, 500); // 每500条一批

// 批量更新
List<Contract> contracts = new ArrayList<>();
// ... 构建数据
contractService.updateBatchById(contracts);

// 4. 逻辑删除
// 实体类配置
@TableLogic
private Integer delFlag; // 0:未删除 1:已删除

// 配置
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: delFlag
      logic-delete-value: 1    # 删除后值
      logic-not-delete-value: 0 # 未删除值

// 使用：delete操作自动变成update
elderMapper.deleteById(1L);
// 实际执行: UPDATE elder SET del_flag = 1 WHERE id = 1
```

</details>

<details>
<summary><b>自动填充</b></summary>

```java
// 5. 自动填充
@Component
public class MetaObjectHandler implements com.baomidou.mybatisplus.core.handlers.MetaObjectHandler {

    @Override
    public void insertFill(MetaObject metaObject) {
        this.strictInsertFill(metaObject, "createTime", LocalDateTime.class, LocalDateTime.now());
        this.strictInsertFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());
        this.strictInsertFill(metaObject, "createBy", String.class, getCurrentUser());
    }

    @Override
    public void updateFill(MetaObject metaObject) {
        this.strictUpdateFill(metaObject, "updateTime", LocalDateTime.class, LocalDateTime.now());
        this.strictUpdateFill(metaObject, "updateBy", String.class, getCurrentUser());
    }

    private String getCurrentUser() {
        try {
            return SecurityUtils.getUsername();
        } catch (Exception e) {
            return "system";
        }
    }
}

// 实体类配置
@Data
public class BaseEntity {
    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;

    @TableField(fill = FieldFill.INSERT)
    private String createBy;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private String updateBy;
}
```

</details>

**MyBatis-Plus特性总览**：

```
┌─────────────────────────────────────────────────────────────┐
│                       查询增强                               │
├─────────────────────────────────────────────────────────────┤
│ LambdaQueryWrapper    │ 分页插件          │ 条件性查询       │
│ 类型安全条件构造      │ 自动添加LIMIT     │ 动态添加条件     │
└──────────┬────────────┴─────────┬─────────┴────────┬────────┘
           │                      │                  │
           v                      v                  v
┌─────────────────────────────────────────────────────────────┐
│                       操作增强                               │
├─────────────────────────────────────────────────────────────┤
│ 批量操作              │ 逻辑删除          │ 自动填充         │
│ saveBatch/updateBatch │ delete变update    │ createTime等     │
└───────────────────────┴───────────────────┴──────────────────┘
           │                      │                  │
           └──────────────────────┼──────────────────┘
                                  v
                    ┌─────────────────────────┐
                    │   简化开发 / 提高效率   │
                    └─────────────────────────┘
```

**单元测试**：

<details>
<summary><b>MybatisPlusTest.java - 单元测试</b></summary>

```java
@SpringBootTest
public class MybatisPlusTest {

    @Autowired
    private ElderMapper elderMapper;

    @Autowired
    private DeviceMapper deviceMapper;

    /**
     * 测试LambdaQueryWrapper
     */
    @Test
    public void testLambdaQueryWrapper() {
        // 条件查询
        List<Elder> elders = elderMapper.selectList(
            Wrappers.<Elder>lambdaQuery()
                .eq(Elder::getStatus, 4)  // 入住中
                .orderByDesc(Elder::getCreateTime)
        );
        assertThat(elders).isNotNull();

        // 条件性查询
        String name = null;
        List<Elder> result = elderMapper.selectList(
            Wrappers.<Elder>lambdaQuery()
                .like(StringUtils.isNotEmpty(name), Elder::getName, name)
        );
        // name为null时，like条件不生效
        assertThat(result).isNotNull();
    }

    /**
     * 测试分页查询
     */
    @Test
    public void testPagination() {
        Page<Device> page = new Page<>(1, 10);
        Page<Device> result = deviceMapper.selectPage(page,
            Wrappers.<Device>lambdaQuery()
                .orderByDesc(Device::getCreateTime)
        );

        assertThat(result.getRecords()).isNotNull();
        assertThat(result.getTotal()).isGreaterThanOrEqualTo(0);
        assertThat(result.getCurrent()).isEqualTo(1);
        assertThat(result.getSize()).isEqualTo(10);

        System.out.println("总记录数: " + result.getTotal());
        System.out.println("总页数: " + result.getPages());
    }

    /**
     * 测试批量操作
     */
    @Test
    @Transactional
    public void testBatchOperation() {
        List<DeviceData> dataList = new ArrayList<>();
        for (int i = 0; i < 100; i++) {
            DeviceData data = new DeviceData();
            data.setIotId("test_device");
            data.setFunctionId("func_" + i);
            data.setValue(String.valueOf(i));
            dataList.add(data);
        }

        long start = System.currentTimeMillis();
        deviceDataService.saveBatch(dataList, 50); // 每50条一批
        long end = System.currentTimeMillis();

        System.out.println("批量插入100条耗时: " + (end - start) + "ms");
        assertThat(end - start).isLessThan(2000); // 2秒内完成
    }
}
```

</details>

**详细解析**：

1. **LambdaQueryWrapper优势**：
   - 类型安全：编译期检查字段名
   - 重构友好：IDE自动重命名
   - 条件性查询：根据参数动态添加条件

2. **分页插件原理**：
   - 拦截SQL，自动添加LIMIT子句
   - 同时执行COUNT查询获取总数
   - 支持多种数据库方言

3. **逻辑删除**：
   - 物理删除变逻辑删除
   - 查询自动过滤已删除数据
   - 适合需要数据恢复的场景

**面试思维链**：

```
面试官问：MyBatis-Plus用了哪些高级特性？

思维链：
1. 先说LambdaQueryWrapper：类型安全，避免硬编码
2. 再说分页插件：自动分页，多数据库支持
3. 再说逻辑删除和自动填充：减少重复代码

回答要点：
"项目中用了MyBatis-Plus的多个高级特性：
1. LambdaQueryWrapper：类型安全的条件构造，避免字段名硬编码
2. 分页插件：Page对象自动分页，支持MySQL等多种数据库
3. 逻辑删除：delete操作自动变成update，数据可恢复
4. 自动填充：createTime/updateTime自动设置
5. 批量操作：saveBatch/updateBatchById，支持分批提交
"

追问应对：
Q: LambdaQueryWrapper和普通QueryWrapper有什么区别？
A: LambdaQueryWrapper通过Lambda表达式引用字段，编译期校验，重构时自动更新字段名，避免硬编码错误

Q: 逻辑删除的数据怎么彻底删除？
A: 1. 物理删除需要用deleteById绕过逻辑删除
   2. 一般用于数据归档、合规清理等场景
   3. 普通业务不建议物理删除，保留历史记录
```

---

### 第28题：Spring Security与RBAC权限

**题目描述**：
请分析项目中Spring Security的配置和RBAC权限模型，包括：用户认证流程、角色权限校验、菜单权限控制。

**核心代码实现**：

<details>
<summary><b>SecurityConfig.java - Spring Security配置</b></summary>

```java
// SecurityConfig.java - Spring Security配置
@Configuration
@EnableGlobalMethodSecurity(prePostEnabled = true)
public class SecurityConfig extends WebSecurityConfigurerAdapter {

    @Autowired
    private UserDetailsService userDetailsService;

    @Autowired
    private TokenService tokenService;

    /**
     * 配置HTTP安全策略
     */
    @Override
    protected void configure(HttpSecurity http) throws Exception {
        http
            // CSRF禁用（前后端分离）
            .csrf().disable()

            // 异常处理
            .exceptionHandling()
                .authenticationEntryPoint((request, response, e) -> {
                    response.setStatus(401);
                    response.setContentType("application/json");
                    response.getWriter().write(
                        JSON.toJSONString(AjaxResult.error(401, "认证失败"))
                    );
                })

            .and()
            // 不创建Session
            .sessionManagement()
                .sessionCreationPolicy(SessionCreationPolicy.STATELESS)

            .and()
            // 请求权限配置
            .authorizeRequests()
                // 允许匿名访问
                .antMatchers("/login", "/captchaImage", "/register").anonymous()
                // 静态资源
                .antMatchers("/profile/**", "/common/**").permitAll()
                // 其他请求需要认证
                .anyRequest().authenticated()

            .and()
            // 添加Token过滤器
            .addFilterBefore(jwtAuthenticationTokenFilter(),
                UsernamePasswordAuthenticationFilter.class);
    }

    /**
     * JWT认证过滤器
     */
    @Bean
    public JwtAuthenticationTokenFilter jwtAuthenticationTokenFilter() {
        return new JwtAuthenticationTokenFilter();
    }

    /**
     * 密码加密器
     */
    @Bean
    public BCryptPasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

</details>

<details>
<summary><b>JwtAuthenticationTokenFilter.java - JWT过滤器</b></summary>

```java
// JwtAuthenticationTokenFilter.java - JWT过滤器
@Component
public class JwtAuthenticationTokenFilter extends OncePerRequestFilter {

    @Autowired
    private TokenService tokenService;

    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                    HttpServletResponse response,
                                    FilterChain chain)
            throws ServletException, IOException {

        // 1. 获取Token
        String token = request.getHeader("Authorization");
        if (StringUtils.isEmpty(token)) {
            chain.doFilter(request, response);
            return;
        }

        // 2. 解析Token获取用户信息
        LoginUser loginUser = tokenService.getLoginUser(token);
        if (loginUser == null) {
            chain.doFilter(request, response);
            return;
        }

        // 3. 设置认证信息到Security上下文
        UsernamePasswordAuthenticationToken authenticationToken =
            new UsernamePasswordAuthenticationToken(
                loginUser, null, loginUser.getAuthorities());
        authenticationToken.setDetails(
            new WebAuthenticationDetailsSource().buildDetails(request));

        SecurityContextHolder.getContext().setAuthentication(authenticationToken);

        chain.doFilter(request, response);
    }
}
```

</details>

<details>
<summary><b>RBAC权限模型 & 权限校验</b></summary>

```java
// RBAC权限模型
// 用户(SysUser) → 角色(SysRole) → 菜单(SysMenu) → 权限标识(perms)

// 权限校验注解使用
@RestController
@RequestMapping("/nursing/elder")
public class ElderController {

    @PreAuthorize("@ss.hasPermi('nursing:elder:list')")
    @GetMapping("/list")
    public AjaxResult list() {
        // 只有拥有 nursing:elder:list 权限的用户才能访问
    }

    @PreAuthorize("@ss.hasPermi('nursing:elder:add')")
    @PostMapping
    public AjaxResult add() {
        // 只有拥有 nursing:elder:add 权限的用户才能访问
    }

    @PreAuthorize("@ss.hasPermi('nursing:elder:remove')")
    @DeleteMapping("/{ids}")
    public AjaxResult remove() {
        // 只有拥有 nursing:elder:remove 权限的用户才能访问
    }
}

// 权限服务
@Component("ss")
public class PermissionService {

    /**
     * 验证用户是否拥有某权限
     */
    public boolean hasPermi(String permission) {
        // 超级管理员直接放行
        if (SecurityUtils.isAdmin()) {
            return true;
        }

        // 获取当前用户权限列表
        Set<String> permissions = SecurityUtils.getLoginUser().getPermissions();
        return permissions.contains(permission);
    }

    /**
     * 验证用户是否拥有某角色
     */
    public boolean hasRole(String role) {
        if (SecurityUtils.isAdmin()) {
            return true;
        }

        Set<String> roles = SecurityUtils.getLoginUser().getRoles();
        return roles.contains(role);
    }
}
```

</details>

**RBAC权限模型**：

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│  SysUser    │       │  SysRole    │       │  SysMenu    │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ user_id(PK) │       │ role_id(PK) │       │ menu_id(PK) │
│ user_name   │       │ role_name   │       │ menu_name   │
│ password    │       │ role_key    │       │ perms       │
└──────┬──────┘       └──────┬──────┘       └──────┬──────┘
       │                     │                     │
       │    ┌────────────────┼────────────────┐    │
       │    │                │                │    │
       v    v                v                v    v
┌─────────────┐       ┌─────────────┐
│SysUserRole  │       │SysRoleMenu  │
├─────────────┤       ├─────────────┤
│ user_id(FK) │       │ role_id(FK) │
│ role_id(FK) │       │ menu_id(FK) │
└─────────────┘       └─────────────┘

用户 ────> 角色 ────> 菜单 ────> 权限标识(如 nursing:elder:list)
```

**JWT认证流程**：

```
用户              JWT过滤器        SecurityContext       TokenService        Redis         Controller
  │                  │                   │                   │                │               │
  │──请求携带Token────>│                  │                   │                │               │
  │                  │──解析Header获取Token                  │                │               │
  │                  │──getLoginUser(token)────────────────>│                │               │
  │                  │                   │                   │──获取缓存────>│               │
  │                  │                   │                   │<─LoginUser────│               │
  │                  │<─LoginUser──────────────────────────│                │               │
  │                  │──setAuthentication(loginUser)────────>│                │               │
  │                  │                   │ [存储认证信息]    │                │               │
  │                  │──────────────────────────────继续过滤链──────────────────────────────>│
  │                  │                   │                   │                │               │
  │                  │                   │<─getLoginUser()──────────────────────────────────│
  │                  │                   │ [返回当前用户信息] │                │               │
  │<───────────────────────────────────────────────────────────业务响应───────────────────────│
```

**前后台认证隔离架构**：

```
┌─────────────────────────────────────────────────────────────┐
│                      后台管理端                              │
├─────────────────────────────────────────────────────────────┤
│ Spring Security ──> JWT Token ──> Redis Session             │
│                                               │              │
│                               @PreAuthorize权限校验          │
└───────────────────────────────────────────────┬─────────────┘
                                                │
                               ┌────────────────┴────────────────┐
                               │   各自独立的用户体系            │
                               └────────────────┬────────────────┘
                                                │
┌───────────────────────────────────────────────┴─────────────┐
│                      微信小程序端                             │
├─────────────────────────────────────────────────────────────┤
│ MemberInterceptor ──> JWT Token ──> ThreadLocal存储userId   │
│                                               │              │
│                               手动权限校验                   │
└─────────────────────────────────────────────────────────────┘
```

**详细解析**：

1. **RBAC模型**：
   ```
   用户(SysUser) ←→ 角色(SysRole) ←→ 菜单(SysMenu)
                                      ↓
                                 权限标识(perms)
   ```

2. **Spring Security核心组件**：
   - `SecurityContextHolder`：存储认证信息
   - `Authentication`：认证对象
   - `UserDetailsService`：加载用户信息
   - `GrantedAuthority`：权限

3. **前后台认证隔离**：
   - 后台：Spring Security + JWT + Redis
   - 前台（小程序）：自定义拦截器 + JWT + ThreadLocal

**面试思维链**：

```
面试官问：权限控制怎么做的？

思维链：
1. 先说权限模型：RBAC（用户→角色→菜单→权限标识）
2. 再说技术实现：Spring Security + JWT + 注解
3. 重点说前后台隔离：两套认证体系

回答要点：
"权限控制基于RBAC模型：
用户 → 角色 → 菜单 → 权限标识(如 nursing:elder:list)

技术实现：
1. Spring Security做认证和授权
2. JWT Token做无状态认证
3. @PreAuthorize注解做方法级权限控制
4. 自定义PermissionService做权限校验

前后台认证隔离：
- 后台：Spring Security + JWT存Redis
- 小程序：自定义MemberInterceptor + JWT + ThreadLocal
"

追问应对：
Q: JWT Token怎么防止被窃取？
A: 1. Token存Redis，可随时撤销
   2. 设置合理过期时间（后台2小时，小程序7天）
   3. HTTPS传输加密
   4. 用户主动退出时删除Redis中的Token

Q: 权限变更后怎么实时生效？
A: 1. Redis缓存权限数据，变更时清除缓存
   2. 下次请求时重新加载权限
   3. 关键权限变更可主动推送通知
```

---

### 第29题：项目性能优化总结

**题目描述**：
请总结项目中所有的性能优化措施，包括：Redis缓存、数据库索引、异步处理、连接池配置等。

**优化措施汇总**：

<details>
<summary><b>Redis缓存优化</b></summary>

```java
// ========== 1. Redis缓存优化 ==========

// 1.1 基础数据缓存（护理等级、护理项目、护理计划）
// 读多写少数据，旁路缓存模式
@Override
public List<NursingLevelVo> listAllNursingLevels() {
    // 先查Redis → 未命中查MySQL → 回填Redis
    String cached = redisTemplate.opsForValue().get(CacheConstants.NURSING_LEVEL_ALL);
    if (StringUtils.isNotEmpty(cached)) {
        return JSONUtil.toList(cached, NursingLevelVo.class);
    }
    // 查数据库并回填...
}

// 1.2 IoT产品列表缓存
// 第三方数据，避免频繁调用华为云API
redisTemplate.opsForValue().set(
    CacheConstants.IOT_ALL_PRODUCT_LIST,
    JSONUtil.toJsonStr(products)
);

// 1.3 设备最新数据缓存
// Hash结构，按iotId快速定位
redisTemplate.opsForHash().put(
    CacheConstants.IOT_DEVICE_LAST_DATA, iotId, jsonStr
);

// 1.4 体检报告临时缓存
// 24小时过期，避免重复解析PDF
redisTemplate.opsForHash().put("healthReport", idCard, content);
redisTemplate.expire("healthReport", 24, TimeUnit.HOURS);
```

</details>

<details>
<summary><b>数据库索引优化</b></summary>

```java
// ========== 2. 数据库索引优化 ==========

// 2.1 唯一索引（高频查询 + 唯一性保证）
ALTER TABLE elder ADD UNIQUE INDEX uk_id_card_no (id_card_no);
ALTER TABLE device ADD UNIQUE INDEX uk_iot_id (iot_id);

// 2.2 普通索引（筛选条件）
ALTER TABLE elder ADD INDEX idx_status (status);
ALTER TABLE elder ADD INDEX idx_bed_id (bed_id);

// 2.3 复合索引（多列组合查询）
ALTER TABLE device ADD INDEX idx_location_product
    (binding_location, product_key, location_type);
```

</details>

<details>
<summary><b>异步处理优化</b></summary>

```java
// ========== 3. 异步处理优化 ==========

// 3.1 AMQP消息异步处理
// 消息接收和业务处理解耦
executorService.submit(() -> processMessage(message));

// 3.2 线程池配置
@Bean("healthAssessmentExecutor")
public Executor healthAssessmentExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(2);
    executor.setMaxPoolSize(5);
    executor.setQueueCapacity(100);
    executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
    return executor;
}
```

</details>

<details>
<summary><b>连接池配置 & 批量操作优化</b></summary>

```java
// ========== 4. 连接池优化 ==========

// 4.1 数据库连接池（HikariCP）
spring:
  datasource:
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

// 4.2 Redis连接池（Lettuce）
spring:
  redis:
    lettuce:
      pool:
        min-idle: 0
        max-idle: 8
        max-active: 8
        max-wait: -1ms

// 4.3 AMQP多连接
connectionCount: 4  // 4个连接提高消费能力


// ========== 5. 批量操作优化 ==========

// 5.1 批量插入设备数据
deviceDataService.saveBatch(dataList, 500); // 每500条一批

// 5.2 批量更新合同状态
for (Contract contract : pendingContracts) {
    contract.setContractStatus(ContractStatus.ACTIVE.getCode());
}
contractService.updateBatchById(pendingContracts);
```

</details>

<details>
<summary><b>查询优化</b></summary>

```java
// ========== 6. 查询优化 ==========

// 6.1 分页查询（避免全表扫描）
Page<Device> page = new Page<>(pageNum, pageSize);
Page<Device> result = deviceMapper.selectPage(page, wrapper);

// 6.2 条件性查询（避免无效条件）
wrapper.like(StringUtils.isNotEmpty(name), Elder::getName, name);

// 6.3 只查需要的字段
@Select("SELECT id, name, status FROM elder WHERE bed_id = #{bedId}")
List<Elder> selectByBedIdSimple(@Param("bedId") Long bedId);
```

</details>

**性能优化全景图**：

```
┌─────────────────────────────────────────────────────────────┐
│                       缓存优化                               │
├─────────────────────────────────────────────────────────────┤
│ 基础数据缓存    IoT产品列表缓存   设备最新数据缓存   体检报告缓存 │
└──────────┬──────────────────────────────────────────────────┘
           │
           v
┌─────────────────────────────────────────────────────────────┐
│                       数据库优化                             │
├─────────────────────────────────────────────────────────────┤
│ 唯一索引        普通索引            复合索引                 │
└──────────┬──────────────────────────────────────────────────┘
           │
           v
┌─────────────────────────────────────────────────────────────┐
│                       异步处理                               │
├─────────────────────────────────────────────────────────────┤
│ AMQP异步消费                    线程池处理                  │
└──────────┬──────────────────────────────────────────────────┘
           │
           v
┌─────────────────────────────────────────────────────────────┐
│                       批量操作                               │
├─────────────────────────────────────────────────────────────┤
│ 批量插入                        批量更新                    │
└──────────┬──────────────────────────────────────────────────┘
           │
           v
┌─────────────────────────────────────────────────────────────┐
│              性能提升10x~100x / 吞吐提升5x / 写入效率10x     │
└─────────────────────────────────────────────────────────────┘
```

**性能优化效果对比**：

| 优化项 | 优化前 | 优化后 | 提升 |
|--------|--------|--------|------|
| 护理等级查询 | 每次查MySQL (~50ms) | Redis缓存 (~5ms) | 10x |
| IoT产品列表 | 每次调华为云API (~500ms) | Redis缓存 (~5ms) | 100x |
| 设备数据查询 | 每次调华为云API (~300ms) | Redis缓存 (~5ms) | 60x |
| PDF解析 | 每次评估都解析 (~3s) | 仅上传时解析1次 | 减少90%+ |
| AMQP消息处理 | 同步处理阻塞连接 | 线程池异步处理 | 吞吐提升5x+ |
| 数据库写入 | 逐条insert | 批量insert | 10x+ |

**面试思维链**：

```
面试官问：项目做了哪些性能优化？

思维链：
1. 先说分类：缓存优化、数据库优化、异步优化
2. 再说具体措施：每类举1-2个例子
3. 重点说效果：有数据支撑

回答要点：
"性能优化主要从四个维度：

1. Redis缓存（最重要）：
   - 基础数据缓存：护理等级、护理项目（10x提升）
   - IoT产品列表缓存：避免频繁调华为云API（100x提升）
   - 设备最新数据缓存：减少API调用（60x提升）
   - 体检报告缓存：避免重复解析PDF

2. 数据库优化：
   - 唯一索引：身份证号、iotId
   - 复合索引：位置+产品组合查询
   - 分页查询：避免全表扫描

3. 异步处理：
   - AMQP消息线程池处理
   - 健康评估可异步化

4. 批量操作：
   - 设备数据批量入库（500条/批）
   - 合同状态批量更新
"

追问应对：
Q: Redis缓存和MySQL数据一致性怎么保证？
A: 1. 旁路缓存模式：先删缓存再更新数据库
   2. 关键数据用分布式锁防止并发脏数据
   3. 低频变更数据可接受短暂不一致

Q: 性能优化后怎么验证效果？
A: 1. 压测对比优化前后的QPS和响应时间
   2. 监控大盘观察日常性能指标
   3. 日志分析慢查询和慢请求
```

---

### 第30题：项目面试模拟 - 完整自我介绍

**题目描述**：
请准备一份完整的项目面试自我介绍，涵盖：项目背景、技术栈、核心模块、技术亮点、个人贡献。

**项目架构全景**：

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层                                │
├──────────────────────────────┬──────────────────────────────┤
│ Vue2 + Element UI            │ 微信小程序                   │
│ (后台管理端)                  │ (家属端)                     │
└──────────────────────────────┴──────────────────────────────┘
           │                              │
           └──────────────┬───────────────┘
                          v
┌─────────────────────────────────────────────────────────────┐
│                        后端层                                │
├─────────────────────────────────────────────────────────────┤
│ Spring Boot ──> MyBatis-Plus ──> Spring Security            │
└─────────────────────────────────────────────────────────────┘
                          │
           ┌──────────────┴───────────────┐
           v                              v
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                │
├──────────────────────────────┬──────────────────────────────┤
│ MySQL                        │ Redis                        │
└──────────────────────────────┴──────────────────────────────┘
                          │
                          v
┌─────────────────────────────────────────────────────────────┐
│                      外部服务                                │
├─────────────────────────────────────────────────────────────┤
│ 百度千帆AI │ 华为云IoTDA │ 微信开放平台 │ 阿里云OSS         │
└─────────────────────────────────────────────────────────────┘
```

**1分钟版本**：

```
智颐养老系统是一个面向养老院场景的综合管理平台，分为后台管理端和微信小程序端。
后台主要负责老人入住、护理服务、健康评估、合同和设备管理，
小程序端主要给家属做微信登录、预约和查询。

技术栈是 Spring Boot + Vue + MyBatis-Plus + MySQL + Redis，
并对接了百度千帆AI大模型和华为云IoT平台。

我主要参与了三个部分：
第一是健康评估模块，把PDF体检报告解析后交给百度千帆做结构化分析，
再把健康分数、风险等级和异常建议落库；
第二是小程序登录模块，基于微信code获取openid和手机号，
再结合JWT与ThreadLocal做前台认证；
第三是IoT设备接入模块，对接华为云IoTDA，
实现设备注册、设备影子查询和AMQP异步接收设备数据。

整个项目虽然是单体架构，但业务链路比较完整，
也接入了AI和IoT两类外部能力。
```

**3分钟深挖版本**：

```
【项目定位】
这是一个面向养老院场景的综合管理系统，包含后台管理端和微信小程序端。
核心业务是老人入住、护理服务、健康评估和智能设备接入。

【技术栈】
后端：Spring Boot + MyBatis-Plus + MySQL + Redis
前端：Vue2 + Element UI
外部对接：百度千帆AI大模型 + 华为云IoTDA + 微信开放平台 + 阿里云OSS

【我负责的模块】

1. 健康评估模块（最有亮点）
用户上传PDF体检报告 → OSS存储 + PDFBox解析 → Redis缓存24小时 →
提交评估时构建强约束Prompt → 调用百度千帆 → 解析JSON → 落库保存

设计亮点：
- Prompt强约束：要求AI只输出固定字段的JSON
- AI+规则分离：AI做认知分析，后端做规则决策
- 响应时间从人工8秒降到AI分析1秒

2. 微信小程序登录模块
微信code → openid → 手机号 → 自动注册 → JWT Token →
拦截器解析Token → ThreadLocal存储userId → 业务代码直接获取

设计亮点：
- 登录即注册：降低用户门槛
- ThreadLocal生命周期管理：afterCompletion中remove()防内存泄漏
- 前后台认证隔离：两套拦截器，互不干扰

3. IoT设备接入模块
华为云IoTDA对接 → 产品同步到Redis → 设备注册三重校验 →
AMQP异步接收设备数据 → 线程池处理 → 批量入库

设计亮点：
- 三重校验：设备名称、NodeId、位置+产品组合
- 多连接AMQP：4个连接提高消费能力
- 时区转换：UTC → 上海时区

【我对这个项目的理解】
这个项目不是特别重架构的项目，但它很适合体现：
1. 业务理解能力：养老院入住、护理、健康评估全流程
2. 第三方平台接入能力：AI、IoT、微信、OSS
3. 工程化能力：缓存设计、异常处理、性能优化
```

**面试常见追问应对**：

```
Q: 项目最大的技术难点是什么？
A: 最大的难点不是调通某个API，而是让AI输出稳定接入业务系统。
   我通过三层控制：Prompt约束 + SDK JSON模式 + 后端反序列化校验。
   另外IoT设备云端和本地数据一致性也是一个需要持续优化的问题。

Q: 如果重新设计这个项目，你会怎么改进？
A: 1. 健康评估改为异步处理，提升用户体验
   2. IoT设备注册增加补偿机制，保证云端本地一致性
   3. 引入API网关统一对外接口
   4. 增加监控和告警体系

Q: 你在团队中是什么角色？
A: 我是后端开发，主要负责健康评估、小程序登录、IoT设备接入三个模块。
   从需求分析、技术方案设计、编码实现到单元测试都有参与。

Q: 项目上线了吗？用户量多少？
A: 项目处于开发阶段，已完成核心功能开发。
   可以按实际项目情况补充说明。

Q: 你从项目中学到了什么？
A: 1. AI集成：不是简单调API，而是如何让AI输出稳定接入业务系统
   2. IoT平台对接：云端和本地数据一致性的挑战
   3. 缓存设计：什么数据适合缓存、如何保证一致性
   4. 安全意识：ThreadLocal内存泄漏、Token安全等
```

---

## 附录：30道题目索引

| 编号 | 题目 | 所属模块 | 难度 |
|------|------|---------|------|
| 1 | 项目模块分层架构设计 | 架构设计 | ★★★ |
| 2 | 单体架构 vs 微服务架构选择 | 架构设计 | ★★★★ |
| 3 | Redis缓存设计与应用 | 架构设计 | ★★★ |
| 4 | 数据库索引设计与优化 | 架构设计 | ★★★ |
| 5 | 定时任务设计与实现 | 架构设计 | ★★★ |
| 6 | PDF体检报告上传与解析 | 健康评估 | ★★★ |
| 7 | AI大模型调用与Prompt工程 | 健康评估 | ★★★★★ |
| 8 | 身份证信息提取与业务规则 | 健康评估 | ★★ |
| 9 | 健康评估完整流程集成测试 | 健康评估 | ★★★★ |
| 10 | 健康评估模块优化方案 | 健康评估 | ★★★★ |
| 11 | 微信小程序登录完整链路 | 登录认证 | ★★★ |
| 12 | ThreadLocal实现用户上下文 | 登录认证 | ★★★★★ |
| 13 | JWT Token生成与验证 | 登录认证 | ★★★ |
| 14 | 前后台用户体系隔离设计 | 登录认证 | ★★★★ |
| 15 | 认证安全增强方案 | 登录认证 | ★★★★ |
| 16 | 华为云IoT平台集成 | IoT设备 | ★★★★ |
| 17 | AMQP设备数据接收 | IoT设备 | ★★★★★ |
| 18 | 设备绑定位置设计 | IoT设备 | ★★★ |
| 19 | 设备数据查询与展示 | IoT设备 | ★★★ |
| 20 | 设备管理完整业务流程 | IoT设备 | ★★★ |
| 21 | 老人入住多表联动业务 | 入住护理 | ★★★★ |
| 22 | 护理等级与护理计划管理 | 入住护理 | ★★★ |
| 23 | 合同状态定时更新 | 入住护理 | ★★★ |
| 24 | 护理项目分页查询与详情 | 入住护理 | ★★ |
| 25 | 预约管理功能 | 入住护理 | ★★★ |
| 26 | 全局异常处理设计 | 综合实战 | ★★★ |
| 27 | MyBatis-Plus高级用法 | 综合实战 | ★★★ |
| 28 | Spring Security与RBAC权限 | 综合实战 | ★★★★ |
| 29 | 项目性能优化总结 | 综合实战 | ★★★★ |
| 30 | 项目面试模拟 - 完整自我介绍 | 综合实战 | ★★★★★ |

---

**最后更新时间**：2026-06-16
**适用项目**：智颐养老系统
**适用场景**：Java后端面试项目实战练习